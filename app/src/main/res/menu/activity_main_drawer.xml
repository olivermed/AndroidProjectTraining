<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">

    <group android:checkableBehavior="single">
        <item
            android:id="@+id/nav_home"
            android:icon="@drawable/ic_home"
            android:title="Home" />
        <item
            android:id="@+id/nav_contact"
            android:icon="@drawable/ic_contacts"
            android:title="My friends" />
    </group>

    <group android:title="Communicate"  android:checkableBehavior="single">
        <item
            android:id="@+id/nav_shared"
            android:icon="@drawable/ic_menu_share"
            android:title="Shared" />
        <item
            android:id="@+id/nav_sended"
            android:icon="@drawable/ic_menu_send"
            android:title="@string/TitleAsked" />
    </group>

    <item android:title="Disconnect">
        <menu>
            <item
                android:id="@+id/nav_disconnect"
                android:icon="@drawable/ic_exit_to_app"
                android:title="Disconnect" />
        </menu>
    </item>

</menu>
